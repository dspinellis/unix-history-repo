.th SETENV NEW 3/30/79
.sh NAME
setenv \*- set environment values
.sh SYNOPSIS
.bd setenv
thing
value
.sh DESCRIPTION
.it Setenv
sets the value of the HOME or TERM environment value
to the specified value.
.sh FILES
/etc/htmp \*- environment database
.sh "SEE ALSO"
sethome (VI), ttytype (VI)
.sh DIAGNOSTICS
``Don't know how to do "foo"\^'' \*- you can only set the
values of HOME and TERM.
.sh BUGS
\*-
