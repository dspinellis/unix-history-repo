# This will always be empty of defines in /usr/new/lib/bmac/?ibmacs.
# In this manner, users can put a bibinc.shortlocal in their local directory,
# and bibinc.shortnames will find it.  However, if the user does not
# have a bibinc.shortlocal, bibinc.shortnames will find this one, which has
# no defines.
