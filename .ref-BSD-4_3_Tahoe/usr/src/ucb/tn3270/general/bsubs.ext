/*
 *	@(#)bsubs.ext	3.1  10/29/86
 */

/*
 * External definitions for genbsubs.c (or vaxbsubs.s, or whatever).
 */
extern int
    bunequal(),
    bskip();

extern unsigned char
    *memNSchr();	/* Search for a character ANDED, increment by stride */
