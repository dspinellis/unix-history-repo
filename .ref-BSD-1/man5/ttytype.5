.th TTYTYPE V 9/25/77
.sh NAME
ttytype \- tty type database
.sh DESCRIPTION
.it Ttytype
is a file which map teletypes to their types.
A line in the file is devoted to each teletype.
The first character of each line gives the teletype letter
as returned by
.it ttyn
(III).
This is followed by a two character code giving the teletype type.
The teletype types are mapped to their capabilities by the data base
.it ttycap
(V).
.s3
The format also allows descriptive information to be placed at the end
of the file following a completely empty line.
.sh FILES
/etc/ttytype	data base
.sh SEE\ ALSO
ttycap (V), ttytype (VI), typeof (VII)
.sh AUTHOR
William Joy
.sh FILES
