#  This program tests lib/psusp.s.

procedure main()
   every write(To(1,10))
   write("---")
   every write(To(90,95))
end

procedure To(i,j)
   while i <= j do {
      suspend i
      i +:= 1
      }
end
