record array(a,b,c,d,e,f,g)

procedure dummy(u,v,x,y,z)
   suspend u | v
   return x
end

procedure main()
   write("every repl(repl(\"xxx\",50),write(1 to 100)) ----> ",image(every repl(repl("xxx",50),write(1 to 100))) | "none")
   write("repl(repl(repl(repl(&cset,100),100),100),100) ----> ",image(repl(repl(repl(repl(&cset,100),100),100),100)) | "none")
end
