link "../ilib/collate"

procedure main()
   while line := read() do {
     line2 := read() | stop("data error")
     x := collate(line,line2)
     write(x)
     write(decollate(x,1))
     write(decollate(x,2))
     }
end
