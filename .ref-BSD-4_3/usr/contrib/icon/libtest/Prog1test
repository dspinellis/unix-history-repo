for i in `cat prog1.tlist`
do
        rm -f local/$i.out
        echo Running $i
	../ibin/$i <t-$i.dat >local/$i.out 2>&1
        echo Checking $i
        diff distr/$i.out local/$i.out
	rm -f $i
done
