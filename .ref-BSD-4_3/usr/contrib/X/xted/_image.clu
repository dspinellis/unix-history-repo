_image = cluster is meta, wait, flush

rep = null

meta = proc (b: bool)
    end meta

wait = proc (speed: int) returns (bool)
    return(input$pending())
    end wait

flush = proc ()
    end flush

end _image
