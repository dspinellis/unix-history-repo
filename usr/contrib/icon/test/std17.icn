record array(a,b,c,d,e,f,g)

procedure dummy(u,v,x,y,z)
   suspend u | v
   return x
end

procedure main()
   write("every i := 1 to 50 do write(real(repl(\"2\",i) || \".\")) ----> ",image(every i := 1 to 50 do write(real(repl("2",i) || "."))) | "none")
   write("every i := 1 to 30 do write((repl(\"2\",i) || \".\") + 1) ----> ",image(every i := 1 to 30 do write((repl("2",i) || ".") + 1)) | "none")
   write("1 to 10 by 2e20 ----> ",image(1 to 10 by 2e20) | "none")
end
