link "../ilib/segment"

procedure main()
   local line
   while line := read() do {
      every write(segment(line,&lcase ++ &ucase))
      write("---")
      every write(segment(line,' '))
      write("===")
      }
end
