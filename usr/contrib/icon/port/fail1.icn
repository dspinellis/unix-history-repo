#  This program tests rt/fail.s.

procedure main()
   if 1 = 2 then write("wrong") else write("right")
   if type(i) == "integer" then write("wrong") else write("right")
   i := 0
   while i < 10 do {
      write(i)
      i +:= 1
      }
end
