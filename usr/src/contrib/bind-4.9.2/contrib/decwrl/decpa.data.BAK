@                              in mx    10 pobox1
                               in mx    20 inet-gw-1
                               in mx    20 inet-gw-2
;
; Host Database
; This file should contain the addresses and aliases
; for local hosts that share this file.
; It is used only for "ifconfig" and other operations
; before the nameserver is started.
;
;
;
fs                             in cname fs.home.vix.com.
