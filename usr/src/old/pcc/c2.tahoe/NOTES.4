The old style makefile had some *really* bizarre stuff for building
c2.h, a file that is checked in under SCCS.  The script doesn't look
like it works.  Also, it uses "tokens" as a source, and tokens isn't
SCCS'd.  I've checked tokens in, and I've removed the c2.h script
from the new Makefile.
