#include "defs.h"
/* Dummy routines to handle calls used in Stifplot */

StifCall() {
    Error("StifCall called in CIFPLOT",INTERNAL);
    }

FreeStif() {
    Error("FreeStif called in CIFPLOT",INTERNAL);
    }
