#	@(#)Makefile	5.2 (Berkeley) 3/22/91

MAN8=	MAKEDEV.0 crash.0 format.0 rbootd.0
MANSUBDIR=/hp300

all: ${MAN8}

clean depend lint tags:

cleandir:
	rm -f ${MAN8}

install: maninstall

.include <bsd.prog.mk>
