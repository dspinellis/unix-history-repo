#	@(#)Makefile	5.7 (Berkeley) %G%

MAN8=	adduser.0 intro.0 makedev.0 rc.0 sticky.0
SUBDIR=	man8.vax

all: ${MAN8}

clean depend lint tags:

cleandir:
	rm -f ${MAN8}

install: maninstall

.include <bsd.prog.mk>
