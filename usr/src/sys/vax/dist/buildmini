#! /bin/sh
#	@(#)buildmini	4.1	%G%
#
miniroot=hp1a
minitype=eagle
#
date
umount /dev/${miniroot}
makefs -s 2048 ${miniroot} ${minitype}
fsck /dev/r${miniroot}
mount /dev/${miniroot} /mnt
cd /mnt; sh -x /sys/dist/get
cd /sys/dist; sync
umount /dev/${miniroot}
fsck /dev/${miniroot}
date
