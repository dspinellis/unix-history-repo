#print
As an easier typing test, see if you can just get
the words "echo management" in correctly.  Type them
in response to '%' and then type RETURN followed
by "ready".
#copyin
#user
#uncopyin
#match echo management
#log
#next
3.1a 10
