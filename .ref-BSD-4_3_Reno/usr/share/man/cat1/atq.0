ATQ(1)			    UNIX Reference Manual			ATQ(1)

NNAAMMEE
     aattqq - display the at(1) job queue

SSYYNNOOPPSSIISS
     aattqq [--cc] [--nn] [_n_a_m_e ...]

DDEESSCCRRIIPPTTIIOONN
     AAttqq displays the queue of jobs, created by the at(1) command, which are
     currently awaiting execution.  With no flags, the queue is sorted in the
     order that the jobs will be executed.

     Options available are:

     --cc    the queue is sorted by the time that the at command was given.

     --nn    only the total number of files that are currently in the queue are
	   printed.

     If a name(s) is provided, only those files belonging to that user(s) are
     displayed.

FFIILLEESS
     /_v_a_r/_s_p_o_o_l/_a_t   spool area


SSEEEE AALLSSOO
     at(1), atrm(1), cron(8)

HHIISSTTOORRYY
     AAttqq appeared in the 4.3 BSD release.
