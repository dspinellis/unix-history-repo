


IMPLOG(8)		      1990			IMPLOG(8)



NNAAMMEE
     implog - IMP log interpreter

SSYYNNOOPPSSIISS
     iimmpplloogg [ --DDFFLLccffrr ] [ --hh _h_o_s_t# ] [ --ii _i_m_p# ] [ --ll _l_i_n_k ] [ --tt
     _m_e_s_s_a_g_e-_t_y_p_e ]

DDEESSCCRRIIPPTTIIOONN
     _I_m_p_l_o_g is program which interprets the message log produced
     by _i_m_p_l_o_g_d(8).

     If no arguments are specified, _i_m_p_l_o_g interprets and prints
     every message present in the message file.  Options may be
     specified to force printing only a subset of the logged mes-
     sages.

     --DD   Do not show data messages.

     --FF   Follow the logging process in action.  This flag causes
	  _i_m_p_l_o_g to check for, and display, new logged messages
	  every 5 seconds.

     --LL   Show only those messages received on the link number of
	  the IP protocol.

     --cc   In addition to printing any data messages logged, show
	  the contents of the data in hexadecimal bytes.

     --ff   Follow the logging process in action as with --FF ,, but
	  print the current contents of the log file first.

     --hh   Show only those messages received from the specified
	  host.  (Usually specified in conjunction with an imp.)

     --ii   Show only those messages received from the specified
	  imp.

     --ll   Show only those messages received on the specified
	  ``link''.

     --rr   Print the raw imp leader, showing all fields, in addi-
	  tion to the formatted interpretation according to type.

     --tt   Show only those messages received of the specified mes-
	  sage type.

SSEEEE AALLSSOO
     imp(4), implogd(8)

BBUUGGSS
     Cannot specify multiple hosts, imps, etc.




Printed 7/27/90               June				1



